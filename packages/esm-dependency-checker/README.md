# esm-dependency-checker

## Usage

```
$ npx esm-dependency-checker --help

  Usage
    $ npx esm-dependency-checker [project path (default: `process.cwd()`)]

  Examples
    $ npx esm-dependency-checker
    $ npx esm-dependency-checker path/to/package
```
